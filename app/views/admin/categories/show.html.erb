<div class="container mt-4">
  <div class="row">
    <h2>Catégorie</h2>
    <hr>
    <ul class="list-group list-group-verticale">
      <li class="list-group-item px-3 py-1 d-flex align-items-center flex-grow-1">
              <% if @category.image.attached? %>
                <%= cl_image_tag @category.image.key, class: "img-category-#{@category.nom.parameterize}" %>
              <% end %>
              <%= @category.nom %>
      </li>
    </ul>
    <div class="mt-2">
      <%= link_to admin_categories_path, class: "btn btn-primary" do %>
        <i class="fas fa-arrow-left text-white"></i>
      <% end %>
    </div>
  </div>
</div>
