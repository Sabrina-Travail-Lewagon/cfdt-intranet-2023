<div class="container mt-4">
  <div class="row">
    <h2>Mettre à jour un article</h2>
    <%= form_with model: [:admin, @article] do |f| %>
      <div class="form-group">
        <%= f.label :Titre %>
        <%= f.text_field :title %>
      </div>
      <div class="form-group">
        <%= f.label :Categories %>
        <%= f.collection_select :category, Category.all, :id, :nom, {}, class: "form-select" %>
      </div>

      <div class="form-inputs">
        <%= f.rich_text_area :rich_body %>
      </div>

      <%= f.submit "Enregistrer", {class: "btn btn-primary my-2"} %>
    <% end %>
    <div>
      <%= link_to admin_root_path, class: "btn btn-primary" do %>
        <i class="fas fa-arrow-left"></i> Retour
      <% end %>
    </div>
  </div>
</div>
